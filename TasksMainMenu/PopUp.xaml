<Window x:Class="TasksMainMenu.PopUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PopUp" Height="600" Width="800" Topmost="True" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Grid Name="stmtdGrid" Background="Navy">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".5*"/>
            <ColumnDefinition Width=".5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height=".7*" />
            <RowDefinition Height=".3*" />
        </Grid.RowDefinitions>
        <TextBlock Name="stmtdTextBlock" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Padding="20" FontSize="40" Foreground="White" TextWrapping="WrapWithOverflow">
            Would you like to view your score?
        </TextBlock>
        <Button Name="stmtdYesButton" Grid.Row="1" Grid.Column="0" FontSize="40" Content="Yes" Margin="20" Click="OnYesButtonClicked"/>
        <Button Name="stmtdNoButton" Grid.Row="1" Grid.Column="1" FontSize="40" Content="No" Margin="20" Click="OnNoButtonClicked"/>
    </Grid>
</Window>

